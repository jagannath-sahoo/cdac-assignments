/* Q6 Find Factorial of number
 */

#include<stdio.h>

/*
 *	Find factorial
 *	param: int type
 *	return: int type 
 * */
int fact(int);


int main()
{
	int n1;
	//int fact = 1;
	printf("Enter a number to find out factorial\n");
	scanf("%d",&n1);
	/*
	while(n1!=0)
	{
		fact = fact*(n1-1);
	}
	*/
	printf("Factorial: %d", fact(n1)); 
	return 0;
}


int fact(int a)
{
	int factorial;
	if(a>1)
		fact=a*fact(a-1);
	else
		fact=1;
}	
