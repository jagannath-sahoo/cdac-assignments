/* q5 Find transpose of a matrix:
 * */


#include<stdio.h>

int(*)[3] initMat(int mat(*)[3], int col, int row);
int main()
{
	
	return 0;
}

int(*)[3] initMat(int mat(*)[3], int col, int row)
{

	for(int i = 0; i < col; i++)
	{
		for(int j = 0; j < row; j++)
	}
	return mat;
}
